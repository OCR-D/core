checks:
  python: true

build:
  nodes:
    analysis:
      tests:
        override:
          - py-scrutinizer-run
          -
            command: pylint-run
            use_website_config: false

    tests: true
filter:
  excluded_paths:
    - '*/test/*'
    - 'ocrd/model/ocrd_page_generateds.py'
  dependency_paths:
    - 'lib/*'
