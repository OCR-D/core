process_queue:
  address: rabbitmq-docker-host.ocrd_network_test
  port: 5672
  credentials:
    username: network_test
    password: network_test
  ssh:
    username: root
    path_to_privkey: /ssh_key
  skip_deployment: true
database:
  address: mongodb-docker-host.ocrd_network_test
  port: 27017
  credentials:
    username: network_test
    password: network_test
  ssh:
    username: root
    path_to_privkey: /ssh_key
  skip_deployment: true
hosts:
  - address: processing-server-host.ocrd_network_test
    username: root
    path_to_privkey: /ssh_key
    workers:
      - name: ocrd-dummy
        number_of_instance: 1
        deploy_type: native
